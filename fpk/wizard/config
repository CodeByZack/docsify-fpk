[{
  "stepTitle": "配置 Docsify 文档站",
  "items": [{
    "type": "tips",
    "helpText": "修改后保存即自动重启服务生效。注意：桌面图标的端口固定为 8666；若端口改为其他值，8666 空闲时图标会自动跳转到新端口，若 8666 被占用则请直接用浏览器访问 http://NAS:新端口。"
  }, {
    "type": "text",
    "field": "wizard_port",
    "label": "服务端口（默认 8666）",
    "initValue": "8666",
    "rules": [{ "pattern": "^[0-9]{2,5}$", "message": "请输入 2-5 位数字端口" }]
  }, {
    "type": "text",
    "field": "wizard_docs_path",
    "label": "文档目录",
    "initValue": "",
    "rules": [{ "pattern": "^$|^\\/", "message": "请输入绝对路径（以 / 开头），或留空" }]
  }, {
    "type": "tips",
    "helpText": "留空 = 使用应用共享目录（推荐，免授权）。若填写外部目录（如 /vol1/1000/docs），请先到「应用中心 → 本应用 → 设置 → 目录授权」添加该目录的访问权限，否则应用无法读取文档。"
  }]
}]
